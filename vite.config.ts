import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  base: '/Elevator-simulation/', // Remplace <nom-du-repo> par le nom exact de ton dépôt
});

